import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SidemenuType',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
